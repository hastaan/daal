{
  "name": "daal-shell-tauri",
  "private": true,
  "version": "0.1.0",
  "description": "Tauri shell that loads client-ui/dist. One binary build pipeline for desktop today + Android/iOS via Tauri Mobile later.",
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "tauri build",
    "android:init": "tauri android init",
    "android:dev": "npm run android:patch && tauri android dev",
    "android:patch": "bash ../../tools/patch-android-manifest.sh && bash ../../tools/patch-android-mainactivity.sh && bash ../../tools/patch-android-icons.sh && bash ../../tools/patch-android-signing.sh",
    "android:build": "npm run android:patch && tauri android build",
    "ios:init": "tauri ios init",
    "ios:dev": "tauri ios dev",
    "ios:build": "tauri ios build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "2.10.1"
  }
}
