# Release Snapshot Policy

The repository at `github.com/hastaan/daal` is the public release
repository for Daal.

Each public version is represented by one signed source snapshot commit
and one signed release tag. Release notes live in `CHANGELOG.md`; build
and packaging details live in `docs/build-and-release.md`.

The first public version is `v0.1.0`.
